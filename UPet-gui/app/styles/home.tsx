import { StyleSheet } from "react-native";

export const homeStyles = StyleSheet.create({
    SelectButtonStyles: {
      borderRadius: 30,
      borderWidth:1,
      borderColor:'black',
      padding:8,
      marginRight:10,
    },
  });